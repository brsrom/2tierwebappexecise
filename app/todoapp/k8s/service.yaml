apiVersion: v1
kind: Service
metadata:
  name: todoapp
  namespace: todoapp
  labels:
    app: todoapp
spec:
  type: LoadBalancer
  selector:
    app: todoapp
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
